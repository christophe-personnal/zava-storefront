name: ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  apm-audit:
    name: APM audit (required)
    uses: hackathon-brown-eagle-55/zava-agent-config/.github/workflows/apm-audit.yml@v5.1.2

  build:
    name: Build & test (placeholder)
    runs-on: ubuntu-latest
    needs: apm-audit
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder build
        run: |
          echo "::notice::Skinny demo scaffold — no real Next.js source yet."
          echo "Once the storefront is fleshed out, restore pnpm install/lint/test/build here."
